{
  "buildCommand": "node --experimental-sqlite scripts/build-web-db.mjs && bunx expo export --platform web",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}
