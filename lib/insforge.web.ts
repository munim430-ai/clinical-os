// Web stub for insforge to prevent native modules from bleeding into web builds via shared imports like auth.ts
// The memory explicitly states to create this stub instead of adding @insforge/sdk to package.json

export const insforge = null as any;
export const ifAuth = null as any;
export const ifDb = null as any;
export const ifStorage = null as any;
