// Stub for web platform since @insforge/sdk is only for native apps
export const insforge = {
  auth: {},
  database: {},
  storage: {}
};
export const ifAuth = insforge.auth;
export const ifDb = insforge.database;
export const ifStorage = insforge.storage;
